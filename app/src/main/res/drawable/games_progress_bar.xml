<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android" >

    <item android:id="@android:id/secondaryProgress">
        <clip>
            <shape>
                <corners android:radius="5dip" />

                <solid android:color="@color/games_progress" />
            </shape>
        </clip>

        <color android:color="@color/games_progress" />
    </item>
    <item android:id="@color/games_progress">
        <clip>
            <shape>


                <solid android:color="@color/games_progress" />
            </shape>
        </clip>

        <color android:color="@color/games_progress" />
    </item>

</layer-list>